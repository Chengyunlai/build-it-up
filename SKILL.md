---
name: build-it-up
description: 在开始写代码前，把产品或工程需求整理成通俗易懂的实现方案、Mermaid 图和可评审 issue；适合需要统一理解并设置明确确认节点的需求迭代。
---

# Build It Up：需求落地助手

当用户希望从需求出发迭代项目，并在实现前把目标、方案和行动路径讲清楚时使用本 skill。交付物是经过确认的 issue 和可执行行动指南；写代码属于后续阶段。

## 工作约定

- 使用用户明确指定的输出语言。没有指定时，跟随用户提问所使用的语言；如果语言确实无法判断，只在起草前询问一次。
- 保持用户原有范围。把假设、未知信息和非目标明确写出来，避免 issue 在不知不觉中扩大。
- 这是一个“共识建立”流程，不是一次性报告生成流程。优先帮助人做当前最重要的一个决定，再逐步展开细节。
- 每轮对话最多提出一个需要用户决定的问题。不要在同一条消息里要求用户同时确认多个命令、范围、技术选型或验收标准。
- 这是有边界的共识建立，不是持续访谈。默认最多推进 1 个主要方向确认和 1 个最终共识确认；不要为了“更完整”不断新增问题。
- 只把会改变产品目标、用户可观察行为、工作范围、验收标准或不可逆技术取舍的内容交给用户决定。低风险的命名、文件位置、实现顺序、测试框架和普通技术细节由 AI 采用合理默认值，并记录为假设。
- 第一轮必须主动报告图表能力：Mermaid、`.drawio` 文件 / URL、网页编辑器、浏览器现有画布和 Desktop 当前画布分别达到哪一级；没有真实工具探测和成功回读，就不得声称“已连接”或“可以驱动”。能力报告只是告知，不要为了它额外发起一个确认问题。规则见 [references/drawio-binding.md](references/drawio-binding.md)。
- 第一轮禁止直接输出完整 issue、完整实现指南、长 Mermaid 图、完整风险清单或一串待确认事项；先输出决策卡片。决策卡片格式见 [references/decision-card.md](references/decision-card.md)。
- 每张决策卡片默认必须配一张能让人“一眼看懂”的图；复杂问题可以配两到三张互补图。图的类型由 AI 根据问题自动选择，不要求用户先指定类型。图要先于长篇解释出现。
- 图表必须根据当前要回答的问题选择正确图型，并按任务自动命名。具体选图、官方高质量参考、底部可见名称和质量检查见 [references/diagram-selection.md](references/diagram-selection.md)。
- 用户确认前，可以只读检查仓库并生成草稿；不得写实现代码，不得创建或修改外部 issue，不得修改 draw.io 文档。
- 把明确确认视为硬性关卡。“看起来不错”“批准”或含义同样明确的表述，才允许创建或修改 issue、图表。确认 issue 不等于授权写代码；开始实现仍需用户单独提出。
- 只有在操作确实成功后，才能声称 issue 或 draw.io 文档已创建、已链接或已同步。
- 代码实施完成后的“阶段完成卡”是可选流程，不是默认的额外讨论。完成代码和适用验证后，只询问一次用户是否需要；用户拒绝就直接结束，不再追问。
- 这是一个从思想到代码再回到证据的闭环：issue/设计记录定义意图，example 展示最短公共 API 组装路径，断点指南解释关键状态，验证结果证明行为，commit 固化本次实现。不能只在聊天中解释而不把闭环记录落到仓库。
- Commit 不是随手写的收尾备注，而是闭环中的正式记录。每次提交都要按 [references/commit-recording.md](references/commit-recording.md) 使用统一写法；提交标题和记录正文的主要语言根据用户当前语言确定。
- draw.io 的“生成文件”“打开编辑器”“控制浏览器现有画布”和“控制 Desktop 当前画布”是不同能力，必须按 [references/drawio-binding.md](references/drawio-binding.md) 使用 D0-D4 分级；没有真实工具探测和成功回读，不得声称已连接或已驱动。
- 所有阶段 example 必须归档在项目唯一的 canonical example 根目录下；每个阶段使用独立目录和 README，不得散落在临时目录或多个未说明的路径中。项目 README、context 和 AGENTS / 等价 agents 文件必须说明这个目录、读取顺序、运行入口和同步规则。详细规范见 [references/project-integration.md](references/project-integration.md)。
- 每个阶段 example 必须同时有 `core/` 和 `user_code/` 两部分；`user_code/` 是使用者主线和设计验收入口，`core/` 是对核心实现的解释。若 `user_code/` 不清爽，优先回查公开 API 和组装边界，不能只增加教程文字。详细规范见 [references/example-contract.md](references/example-contract.md)。

## 工作流程

1. 明确事实，但不急于下结论。只读检查仓库，找出当前行为、已有术语、约束和真正未知的信息；同时探测当前环境的图表能力；把“仓库已观察到的事实”“工具实际可用性”和“需要用户决定的选择”分开。
2. 输出第一张决策卡片。只说明当前阶段、目标、图表能力状态、现在唯一要决定的事情、一个最小视觉图、推荐选择、简短理由、本轮不决定什么，以及一个明确问题。若术语不可避免，先用自然语言解释，再括号标注术语。
3. 等待用户回答。用户回答后，判断是否已经具备继续推进的条件；如果具备，直接进入方案卡片或下一项被授权的行动，不要强行发明下一个问题。如果用户提出疑问，先解释疑问，不要趁机继续倾倒后续章节。
4. 逐步形成方案。关键方向确认后，再逐渐补充正常路径、失败路径、范围、验收标准、实现顺序和测试策略。每次新增内容都要说明它解决了前一轮暴露的哪个问题；没有新的高影响决策时，不再暂停等待确认。
5. 在每轮卡片中提供与当前决策匹配的最小图；模型足够稳定后，再增加完整 Mermaid 图或互补视图。图的复杂度随理解程度增加，不能在第一轮用十几个节点制造“看起来很完整”的假象。
6. 每张 Mermaid / draw.io 图都要生成任务名称。文件 / 页面使用稳定身份；画布底部单独放置一条可见名称，描述具体任务结果和图型，例如“图名：输入命令后创建任务｜图型：用户流程｜阶段：stage-01”。底部名称不得挡住主流程，不得只写“流程图”“系统架构”等空泛标题。多图时按视角区分名称。
7. 所有关键决策完成后，按 [references/issue-template.md](references/issue-template.md) 组装完整 issue 草案，并展示一份便于人快速审阅的“最终共识卡片”。此时仍然不要发布 issue，除非用户明确确认。
8. 用户明确确认最终共识后，通过仓库可用的 tracker 创建或更新 issue。使用 issue 标识作为相关图表名称和链接的统一主键。如果没有 tracker，则保存 Markdown issue 产物，并明确说明它只是本地草稿，不能假装已经发布。
9. 如果用户要求连接 draw.io，按 [references/drawio-binding.md](references/drawio-binding.md) 执行。环境支持时，优先使用已连接的桌面版或网页版 diagrams.net；否则提供准确的 Mermaid 源码和 draw.io 导入、互链交接步骤，并明确哪些步骤仍需手工完成。网页版可用时，用户只要求“画图”也应在能力报告后主动提示一次是否导入，不得静默跳过。
10. 完成已确认的 issue 和图表交接后停止，除非用户另外要求实现。如果用户要求实现，先复述已确认的范围，再以 issue 为行动指南开始编码，并在决策变化时同步更新 issue。
11. 如果用户要求了实现，开始编码前先按 [references/project-integration.md](references/project-integration.md) 和 [references/example-contract.md](references/example-contract.md) 发现或建立项目级导航：确定 canonical example 根目录，并检查 README、context、AGENTS / 等价 agents 文件是否说明 example 的位置和读取顺序。为本阶段建立 `core/` 与 `user_code/`，先让 `user_code/` 通过公开 API 跑通真实场景，再补充核心代码说明和断点；代码路径或稳定事实变化时同步更新 context。不要把阶段细节堆进 AGENTS，那里只保留稳定工作规则。
12. 代码修改和适用验证完成后，只发送一次可选提示：“实现已完成，验证结果：<一句话结果>。是否生成阶段完成卡，展示关键路径断点、真实 example、图和验证证据？”用户确认后，按 [references/stage-completion-card.md](references/stage-completion-card.md) 输出，并把阶段完成卡、实现记录和项目级导航更新落到仓库；用户拒绝、没有回复或未要求时，直接给出普通完成摘要，不要重新开启需求访谈。
13. 如果用户明确要求“闭环记录”“把理解路径落到 example / issue / commit”或同等意思，则不再把阶段完成卡当作可选项：实现后直接完成 [references/implementation-loop.md](references/implementation-loop.md) 和 [references/project-integration.md](references/project-integration.md) 要求的记录、链接和验证。

## 讨论停止条件

满足以下条件时，停止继续讨论，转入方案、issue 草案或用户已经授权的下一步行动：

- 用户目标可以用一句话说清楚。
- 本阶段包含什么、不包含什么已经足够明确。
- 至少有一个具体场景、一个可观察结果和一个重要失败或边界处理。
- 剩余未知项不会改变目标、范围或验收标准，或者可以用低风险假设处理。

只有在缺少的信息会导致方向错误、产生重大返工、改变外部行为或影响不可逆决策时，才继续提问。最多再问一个问题；如果用户不回答但已经给出足够方向，使用明确标注的假设继续，不要把任务变成等待用户逐项审批的问卷。

以下内容默认不是继续讨论的理由：

- Python 还是 TypeScript 等尚未影响产品目标的实现选择。
- 普通文件名、函数名、目录名和实现顺序。
- 测试文件如何组织，除非仓库已有强制约定冲突。
- 已经可以从仓库、官方文档或常规工程实践确认的事实。
- 为了“更严谨”而重复确认已经明确的决定。

当用户说“可以”“好的”或表达同等确认时：

1. 只确认上一张卡片中的决定。
2. 检查停止条件。
3. 如果停止条件满足，直接给出下一步产物或行动，不再自动提出新问题。

## 信息展开节奏

严格采用以下阶段，除非用户明确要求跳过中间过程直接查看完整草案：

1. **决策卡片**：帮助用户理解眼前唯一的选择。
2. **理解卡片**：记录已确认的决定，以及它对下一步的影响。
3. **方案卡片**：补充对象关系、关键路径、失败路径和主要取舍。
4. **最终共识卡片**：用短摘要、验收标准和一张图确认整体理解。
5. **完整 issue**：只有在前面的共识稳定后才生成，供发布前最后审阅。

如果用户只问一个术语，只解释这个术语并回到当前决策，不要自动输出完整方案。可以用“原始输入 → 人能理解的中间结果 → 术语名称”的顺序介绍抽象，例如：

```text
用户输入：完成买牛奶
人能理解的中间结果：用户想把“买牛奶”这项任务标记为已完成
项目术语：Todo Intent
```

不要把示例载体误写成产品决策。比如选择 Todo 作为第一个验证应用时，要明确说“这是用于验证框架的示例，不代表产品要做成 Todo”。

## 图表能力告知

第一张决策卡片中固定加入一个简短的能力区块，不要等用户主动问 draw.io：

```text
图表能力：
- Mermaid（D0）：可直接生成并在支持的平台渲染。
- `.drawio` 文件 / URL（D1）：<已确认可生成 / 未确认>。
- draw.io 网页编辑器（D2）：<已打开并读到状态 / 未配置 / 未检测到浏览器控制>。
- 浏览器现有画布（D3）：<已连接并能回读 / 只有打开能力 / 未检测到 live 工具>。
- Desktop 当前画布（D4）：<已连接并能回读 / 未检测到 Desktop live 工具 / 连接失败>。
- 当前交付：<可以直接完成什么；哪些步骤需要用户手工完成>。
```

能力状态必须基于当前任务实际暴露的工具或已成功执行的操作，不得因为仓库有 `drawio-binding.md` 就声称能操作 draw.io。没有专用连接器时，Mermaid 仍然可以正常交付；draw.io 只报告可行的导入、保存和互链路径。

如果 draw.io 能力可用，在用户确认最终方案后可以执行绑定；如果不可用，仍要主动提供准确的 Mermaid 源码、建议文件名、导入方式和互链手册。不要把“用户没有要求 draw.io”理解成“用户不需要知道 draw.io 是否可用”。

上面的“能力可用”必须解释为已经达到目标等级并完成成功回读，而不是仅仅知道某个安装命令。若用户要求实时驱动当前画布，先确认是浏览器画布还是 Desktop 画布，再分别验证 D3 或 D4；D1 文件交付和 D2 打开网页不能替代 D3/D4。

## draw.io 执行硬规则

- 先检查当前宿主实际暴露的 draw.io MCP、浏览器控制或桌面控制工具；没有工具就直接标记为未配置，不进入假想操作。
- 每次连接或写入后必须回读文档 / 页面 / 元素状态，或截图确认目标真的变化；只发出命令不算成功。
- 如果目标等级不可用，停止在已证实的等级，给出 Mermaid、可选 `.drawio` 文件、失败证据和最短配置路径，不要反复讨论或声称“稍后同步”。
- 标准 MCP、MCP App、Codex CLI 插件、浏览器扩展和 Desktop live 通道互不等价；当前 Codex Desktop 不得默认当成 Codex CLI 或 MCP Apps 宿主。

## 具体可视化

文字不能独自承担理解任务，但“有图”不等于“看得懂”。每张决策卡片都要先选择最适合当前问题的视觉表达，再写必要的解释。优先使用 Mermaid，因为它可渲染、可复制、可进入 issue 和 draw.io；用户需要正式编辑时，再按 draw.io 绑定流程交付可编辑图。

图必须展示具体事实，而不是把抽象结论换成方框。每张图至少要包含以下五类信息中的四类，并且必须包含“具体输入”和“可观察结果”：

- 具体的人或调用方：谁在使用它。
- 具体输入：命令、按钮、请求、事件、文件或数据，例如 `add 买牛奶`。
- 具体处理：实际经过的步骤、分支、调用或状态变化。
- 具体结果：终端输出、页面变化、API 响应、保存的记录或错误。
- 验证关系：这个结果证明了哪个能力或验收标准。

如果去掉卡片旁边的解释，读者仍应能从图中回答“用户做了什么、系统做了什么、最后看到了什么”。无法回答时，继续改图，不要用一段更长的文字补救。

禁止使用只有以下结构的空泛图：

```text
目标 → 选择 → 方案 A / 方案 B → 更快 / 更大 / 更好
```

这种图只能复述推荐理由，不能说明要构建什么。涉及“先做验证应用还是直接做完整框架”时，必须把选择落到一个真实场景，例如展示用户输入 `add 买牛奶` 后，应用如何解析、路由、写入任务并输出结果；再用第二张范围图标出这个场景覆盖哪些框架能力、哪些能力明确不覆盖。

根据问题自动选择图型：

- “先做什么、之后发生什么” → 流程图或时间线。
- “谁和谁如何协作” → 时序图或组件关系图。
- “系统有哪些状态、如何变化” → 状态图。
- “当前方案和未来方案是什么关系” → 分层图、范围图或对比图。
- “有哪些选择、推荐哪一个” → 方案分支图或对比图。
- “用户如何完成任务” → 用户旅程图或步骤图。
- “数据如何组织” → ER 图或数据流图。

图必须服务于当前唯一决策，而不是装饰。默认控制在 3–8 个主要元素；如果一张图无法同时表达“具体场景如何运行”和“本阶段验证边界”，拆成两张互补图，例如“具体执行图 + 验证覆盖图”。每张图都要有一个短标题，并在图下只解释读者必须知道的箭头、边界或差异。

决策卡片的视觉顺序固定为：

```text
当前阶段 → 现在只决定什么 → 一眼看懂图 → 推荐与理由 → 本轮不决定什么 → 一个问题
```

不要把 Mermaid 源码放在图的前面阻挡理解；平台能渲染时先展示渲染图，源码放在图后或按需提供。若平台不能渲染，才同时给出源码和一句简短文字回退。

## 迭代行为

当用户修改需求时，不要只改其中一个章节。要同步检查摘要、假设、图表、验收标准、风险和实现顺序，然后重新展示评审包；外部变更前再次等待确认。

当用户问“我们能不能做 X？”时，将其理解为探索和起草，不是写代码授权。用户提供已有 issue 时，把它作为评审和改进的输入；除非用户要求替换，否则保留其标识和链接。

当用户只回复“可以”“好的”这类简短肯定时，只把它解释为对上一张决策卡片的确认，不得顺便确认尚未展示或尚未询问的其他决定。

不要自动调用 `grilling`、`grill-with-docs` 或同类“持续访谈”流程。只有用户明确要求“逐个追问”“压力测试方案”或使用 `grill` 语义时，才切换到这类访谈；一旦用户要求回到 `build-it-up`，立即恢复本 skill 的有限讨论和停止条件。

如果仓库的 `AGENTS.md`、其他 skill 或历史流程要求持续访谈，说明它与当前 `build-it-up` 目标存在流程冲突，并以用户当前要求的有限共识流程为准，不要静默进入无限追问。

## 实施完成后的可选阶段汇报

代码真正完成并通过适用的测试、构建或手工验证后，才可以询问是否生成阶段完成卡。询问本身必须是一个封闭、低负担的问题，不要附带新的设计问题：

```text
实现已完成，验证结果：<一句话结果>。
是否生成阶段完成卡，展示关键路径断点、真实 example、图和验证证据？
```

用户回答“是”时，输出一次阶段完成卡并结束；不要在卡片结尾继续问“下一阶段做什么”。用户回答“否”时，输出普通完成摘要并结束。用户没有明确要求实现时，不要主动提出这个问题。

阶段完成卡必须基于已经完成的代码和已经执行的验证，不能把计划、假设或待实现内容写成完成事实。它至少包含一个能复现的输入和输出，并用流程图、时序图、状态图或架构图中的一种解释关键路径；复杂阶段最多使用两张互补图。用户选择执行时，阶段完成卡必须写入仓库记录并回链 issue、example 和 commit，不能只作为聊天消息存在。默认区分“实现 commit”和“记录 commit”，除非仓库明确采用单一最终 commit 的方式。

## 思想到代码的闭环要求

只要任务进入编码阶段，就维护以下追踪关系：

```text
Issue / 设计记录
    ↓ 定义目标、范围、验收标准
Example
    ↓ 展示最短公共 API 组装和一次真实调用
关键断点
    ↓ 观察输入、状态、依赖和输出
测试 / 手工验证
    ↓ 产生可复现证据
Commit
    ↓ 固化本次实现和变更范围
实现记录
    └── 回链 Issue、Example、断点、验证命令和 Commit
```

实现前，issue 或设计记录必须预先写出：目标 example 路径、最短组装调用、关键断点候选和验证方式。实现时，优先让 example 通过公开 API 运行真实场景；不要只写内部单元测试来代替用户可理解的使用路径。实现后，按 [references/implementation-loop.md](references/implementation-loop.md) 生成或更新记录，并使用真实 commit hash 关联本次结果。提交前读取 [references/commit-recording.md](references/commit-recording.md)，按当前用户语言生成 commit message。

## 工具路由

- 在作出技术判断前，先使用仓库检查工具进行只读发现。
- 进入编码阶段前，阅读 [references/project-integration.md](references/project-integration.md)，识别项目已有的 README、context、AGENTS / 等价 agents 文件和 example 根目录；只在缺少入口且确有需要时建立默认入口。
- 如果仓库配置了 `taskctl`，issue 操作和状态同步遵循 `manage-taskboard` skill。
- 只有用户要求连接网页版或桌面版 draw.io，且环境确实提供相应能力时，才使用应用内浏览器或 computer-use；不要虚构 draw.io API 或 MCP 连接。
- 每次起草 issue 都要阅读 issue 模板参考；每轮需求共识交互都要遵循 [references/decision-card.md](references/decision-card.md)；只有需要将图表绑定到 draw.io 时才阅读 draw.io 绑定参考。
- 进入实现闭环或需要归档阶段 example 时，阅读 [references/implementation-loop.md](references/implementation-loop.md)、[references/project-integration.md](references/project-integration.md) 和 [references/example-contract.md](references/example-contract.md)。
